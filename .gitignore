# =============================================================
# Baby Shower App — .gitignore
# =============================================================

# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# Wrangler build artifacts
.wrangler/
dist/
build/

# Wrangler local development secrets
# This file stores local secret values (like ADMIN_PASSWORD for dev)
# It must NEVER be committed — treat it like a .env file
worker/.dev.vars
.dev.vars

# Environment / secret files — NEVER commit these
.env
.env.local
.env.production
.env.staging
.env.*.local

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
*~
.project
.classpath
.settings/

# TypeScript
*.tsbuildinfo
*.d.ts.map

# Logs
logs/
*.log

# Optional: OS package lock files if you prefer not to track them
# package-lock.json
# yarn.lock
