# =============================================================
# Baby Shower App — Environment Variables Reference
# =============================================================
# This file documents all configuration values needed to run
# the app. DO NOT put real secrets here — this file is committed
# to git as documentation only.
#
# For actual values, see the sections below for where each
# variable is set.
# =============================================================


# -------------------------------------------------------------
# CLOUDFLARE WORKER SECRETS
# Set via Wrangler CLI (never stored in files):
#   wrangler secret put ADMIN_PASSWORD
# -------------------------------------------------------------

# Password to protect the admin panel at /admin.html
# Choose something strong — this is the only thing protecting
# your gift registry from being edited by guests.
ADMIN_PASSWORD=your-secure-admin-password-here


# -------------------------------------------------------------
# CLOUDFLARE WORKER [vars] — set in worker/wrangler.toml
# These are NOT secrets (they're not sensitive) so they live
# in the config file, not as Wrangler secrets.
# -------------------------------------------------------------

# The GitHub Pages origin where your frontend is hosted.
# Used by the Worker for CORS (only allows requests from this domain).
# For local development, wrangler.toml overrides this to "*".
FRONTEND_ORIGIN=https://goncalo-araujo.github.io


# -------------------------------------------------------------
# CLOUDFLARE D1 DATABASE
# Set in worker/wrangler.toml under [[d1_databases]]
# -------------------------------------------------------------

# The D1 database ID — you get this after running:
#   wrangler d1 create babyshower-db
# Then paste the returned ID into wrangler.toml
D1_DATABASE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx


# -------------------------------------------------------------
# LOCAL DEVELOPMENT — worker/.dev.vars
# Wrangler reads this file automatically when running: wrangler dev
# Create this file locally (it is gitignored) with:
# -------------------------------------------------------------
#
#   ADMIN_PASSWORD=local-dev-password
#
# This lets you test the admin panel locally without exposing
# your real password.
